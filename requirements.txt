# Direct PerturbGen dependencies (Python >= 3.11).
#
# Preferred install (matches poetry.lock):
#   cd Perturbgen
#   poetry install
#
# Pip fallback (does not pin the full transitive tree):
#   pip install -r requirements.txt
#
# Versions match pyproject.toml. CUDA wheels for torch come from PyPI/torch
# indexes; NVIDIA packages are not listed here on purpose.

# Core scientific stack
numpy==2.2.0
pandas==2.2.3
scipy==1.14.1
anndata==0.11
scanpy==1.11.2
h5py==3.12.1
pyarrow==18.1.0
numba==0.61.2
llvmlite==0.44.0
scikit-learn==1.5.2
statsmodels==0.14.4
umap-learn==0.5.7
leidenalg==0.10.2
igraph==0.11.8

# Deep learning / training
torch==2.5.1
pytorch-lightning==2.4.0
lightning==2.4.0
torchmetrics==1.4.1
einops==0.8.0
deepspeed==0.15.4
accelerate==1.2.0
peft==0.14.0
scvi-tools==1.3.1.post1

# HuggingFace / tokenization
datasets==3.0.0
transformers==4.47.0
huggingface-hub==0.26.5
tokenizers==0.21.0
safetensors==0.4.5
evaluate==0.4.3
rouge-score>=0.1.2

# CLI, logging, plotting
click==8.1.7
tqdm==4.67.1
PyYAML==6.0.2
wandb==0.17.9
tensorboard==2.18.0
matplotlib==3.9.3
seaborn==0.13.2
pillow==11.0.0

# Notebooks 04-06 (gene programs / enrichment)
gseapy==1.1.4
adjustText==1.3.0
ipykernel==6.29.5
ipython==8.27.0

# Optional training extras used by current scripts
ray==2.40.0
optuna==4.1.0
optuna-integration==4.1.0
mpi4py==4.0.1
sympy==1.13.1

# Dev / tests (also in Poetry group.dev)
pytest==7.4.4
